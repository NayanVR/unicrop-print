services:
    app:
        build:
            context: .
            dockerfile: docker/php/Dockerfile
        restart: unless-stopped
        volumes:
            - app-public:/var/www/html/public
        env_file: .env
        depends_on:
            mysql:
                condition: service_healthy
            garage:
                condition: service_started

    nginx:
        image: nginx:1.27-alpine
        restart: unless-stopped
        # No host port published here on purpose: Dokploy's Traefik proxy
        # already owns 80/443 on the host, and routes to this service over
        # the internal Docker network using its container port (80). If you
        # ever run this stack with plain `docker compose up` (no platform
        # proxy in front), add back `ports: ["${APP_PORT:-8080}:80"]`.
        volumes:
            - app-public:/var/www/html/public:ro
            - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
        depends_on:
            - app

    mysql:
        image: mysql:8.4
        restart: unless-stopped
        environment:
            MYSQL_DATABASE: ${DB_DATABASE:-unicrop_print}
            MYSQL_USER: ${DB_USERNAME:-unicrop}
            MYSQL_PASSWORD: ${DB_PASSWORD:-secret}
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-secret}
        volumes:
            - mysql-data:/var/lib/mysql
        ports:
            - "${FORWARD_DB_PORT:-3306}:3306"
        healthcheck:
            test: [ "CMD", "mysqladmin", "ping", "-h", "localhost" ]
            interval: 5s
            timeout: 5s
            retries: 10

    garage:
        image: dxflrs/garage:v1.0.1
        restart: unless-stopped
        environment:
            GARAGE_RPC_SECRET: ${GARAGE_RPC_SECRET:-changeme0123456789abcdef0123456789abcdef0123456789abcdef01}
            GARAGE_ADMIN_TOKEN: ${GARAGE_ADMIN_TOKEN:-changeme-admin-token}
        volumes:
            - ./docker/garage/garage.toml:/etc/garage.toml
            - garage-meta:/var/lib/garage/meta
            - garage-data:/var/lib/garage/data
            - ./scripts/garage-setup.sh:/scripts/garage-setup.sh

volumes:
    mysql-data:
    garage-meta:
    garage-data:
    app-public:
